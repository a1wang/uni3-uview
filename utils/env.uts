let BASE_URL;

//开发环境中
if (process.env.NODE_ENV === 'development') {
  // 开发环境
  BASE_URL = window.location.origin + '/api';
} else {
  // 生产环境
  BASE_URL = 'https://xxxx';
}
 
export default BASE_URL;